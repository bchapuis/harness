[package]
name = "spec-xref"
description = "Resolves the section citations the specifications make of each other, so a renumbered section fails the build rather than leaving prose pointing at nothing."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true

[lints]
workspace = true
