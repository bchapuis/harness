[package]
name = "tenancy"
description = "An ownership-index grain: principals own and enumerate their grains, layered on granary"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
granary.workspace = true
actor-core.workspace = true
serde.workspace = true

[dev-dependencies]
actor-cluster.workspace = true
actor-simulation.workspace = true
futures.workspace = true
# `testing` for the shared G3/G5 and G6 checkers the swarm attaches — test
# support, never shipped in a production build (docs/simulation-testing.md).
granary = { workspace = true, features = ["testing"] }
serde.workspace = true

[lints]
workspace = true
