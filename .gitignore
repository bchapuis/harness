/target
**/*.rs.bk
*.pdb
Cargo.lock

# macOS finder metadata
.DS_Store

# Local Claude Code tooling state
/.claude

# Runtime data of demo-agent.sh / harness-standalone (journals, workspaces, node logs)
/harness-data

# Runtime data of demo-machine.sh / machine-standalone (journals, disk images,
# node logs, and the demo's own ssh keypair)
/machine-data
